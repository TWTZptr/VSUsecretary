import styled from '@emotion/styled';
import { Box } from '@mui/material';

export const TabFlexbox = styled(Box)(() => ({
  display: 'flex',
  flexDirection: 'row',
  justifyContent: 'start',
  width: '100%',
  height: '100%',
}));
