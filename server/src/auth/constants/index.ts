import { ADMIN } from '../../users/constants';

export const INVALID_CREDENTIALS_MSG = 'Credentials are incorrect';
export const BANNED_USER_MSG = 'You are banned';
export const GLOBAL_ACCESS_ROLES = [ADMIN];
